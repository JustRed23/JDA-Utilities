plugins {
    id 'java-library'
}

group 'dev.JustRed23'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
    maven { url 'https://maven.lavalink.dev/releases' }
}

dependencies {
    compileOnly libs.jda

    implementation libs.jetbrains.annotations

    implementation libs.hikaricp
    implementation libs.sqlite.jdbc

    implementation libs.snakeyaml

    //search sources
    api libs.google.api.services.youtube
    api libs.spotify.web.api.java

    testImplementation libs.junit.jupiter.api
    testRuntimeOnly libs.junit.jupiter.engine
    testRuntimeOnly libs.junit.platform.launcher

    testImplementation libs.jda
    testImplementation libs.logback.classic
    testImplementation(libs.dave.api)
    testImplementation(libs.bundles.dave.natives)
    testImplementation(project(":lavalink"))
    testImplementation(project(":lavaplayer"))
    testImplementation(libs.lavalink.youtube)
}

subprojects {
    apply plugin: 'java-library'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(25)
        }
    }

    repositories {
        mavenCentral()
        maven { url 'https://jitpack.io' }
        maven { url 'https://maven.lavalink.dev/releases' }
    }

    dependencies {
        compileOnly libs.jda

        implementation libs.jetbrains.annotations

        implementation libs.hikaricp
        implementation libs.sqlite.jdbc

        implementation libs.snakeyaml
    }
}

test {
    useJUnitPlatform()
}